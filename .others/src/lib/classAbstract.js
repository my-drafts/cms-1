'use strict';

var type = require('ztype'), of = type.of, is = type.is, like = type.like, ofs = type.ofs;
var util = require('util');

class classAbstract {
	constructor(){
	}

}
module.exports = classAbstract;
